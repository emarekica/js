
const maxTemp1 = [17, 21, 23];
const maxTemp2 = [12, 5, -5, 0, 4];

const maxTemp = maxTemp1.concat(maxTemp2);


/* PROBLEM-SOLVING

    1. What will the function print?

                it will print: "21ºC in 2 days"

    2. How will the function count the days?

                index + 1



                let counter = 0;
                for (let i = 0; i < storage.length; i++) {
                if (storage[i].status === '0') counter++;
                }

console.log(counter); // 6

    3. How to concatenate 2 strings?

                const array1 = ['a', 'b', 'c'];
                const array2 = ['d', 'e', 'f'];
                const array3 = array1.concat(array2);


    4. Transforming array into string.             
*/



